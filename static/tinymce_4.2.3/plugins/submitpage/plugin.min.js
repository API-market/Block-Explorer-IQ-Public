tinymce.PluginManager.add('submitpage', function(editor, url) {
    editor.addButton('submitpage', {
        title: gettext('Submit'),
        text: gettext('Submit'),
        icon: false,
        classes: "btn-submit"
    });

});
