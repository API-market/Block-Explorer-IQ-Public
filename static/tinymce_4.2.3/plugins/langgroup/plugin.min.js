tinymce.PluginManager.add('langgroup', function(editor, url) {
    editor.addButton('langgroup', {
        title: gettext("Connect Languages"),
        text: gettext("Connect Languages"),
        icon: false,
        onclick: null,
        classes: 'btn-langgroup',
    });
});
