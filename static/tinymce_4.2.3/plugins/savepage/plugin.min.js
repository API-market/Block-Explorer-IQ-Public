tinymce.PluginManager.add('savepage', function(editor, url) {
   var self = this, button, menuInstance, menuDOMString;
   var clickedOnce = false;
    
   editor.addButton('savepage', {
      title: gettext('Save'),
      text: gettext('Save'),
      classes: 'btn-save-page',
      icon: 'icon-save',
      onclick: function() {
         $("#edit_blurb :submit").click();
      },
   });

});